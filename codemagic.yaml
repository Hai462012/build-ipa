workflows:
  ios-workflow:
    name: iOS Workflow

    environment:
      xcode: latest

    scripts:
      - npm install
      - chmod +x node_modules/.bin/cap
      - npx cap sync ios

    artifacts:
      - ios/**/*.app